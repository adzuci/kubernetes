nginx-ingress:
  controller:
    ingressClass: public   # Only implement ingresses that are explicitly marked as public
    publishService: 
      enabled: true   # required for external-dns to map elb ingresses to dns names
    extraArgs:
      enable-ssl-passthrough: ""
    service:
      type: NodePort # In development, we set this because there is nothing to create a load balancer.

  # resources:
  #   limits:
  #     cpu: 100m
  #     memory: 250Mi
  #   requests:
  #     cpu: 50m
  #     memory: 100Mi
  # defaultBackend:
  #   resources:
  #     limits:
  #       cpu: 100m
  #       memory: 250Mi
  #     requests:
  #       cpu: 50m
  #       memory: 100Mi
